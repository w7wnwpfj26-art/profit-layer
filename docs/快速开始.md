# Nexus Yield: 智能资产矩阵 (Intelligent Asset Matrix) - 快速開始

## 🎯 5 分鐘快速啟動指南

### 前置要求

- ✅ Docker Desktop 已安裝並運行
- ✅ Node.js 20+ 和 pnpm 9+ 已安裝
- ✅ 至少 4GB 可用內存

### 啟動步驟

#### 1️⃣ 克隆項目（如果還沒有）

```bash
git clone https://github.com/YOUR_ORG/nexus-yield.git
cd nexus-yield
```

#### 2️⃣ 啟動數據庫

```bash
# 一鍵啟動數據庫
bash scripts/start-database.sh

# 或啟動全部基礎服務（數據庫 + Redis + Grafana）
bash scripts/start-database.sh --all
```

等待 5-10 秒，看到 "✓ 數據庫啟動完成！" 即可。

#### 3️⃣ 安裝依賴並啟動 Dashboard

```bash
# 安裝依賴（首次運行）
pnpm install

# 啟動 Dashboard
cd packages/dashboard
pnpm dev
```

#### 4️⃣ 訪問應用

打開瀏覽器訪問：
- **Dashboard**: http://localhost:3002
- **Grafana**: http://localhost:3003 (admin/admin)

同局域網的其他設備也可訪問：使用本機局域網 IP（如 `http://192.168.x.x:3002`）。開發服務已綁定 `0.0.0.0`，無需額外配置。

---

## 📊 查看數據

### 檢查數據庫內容

```bash
# 快速檢查
bash scripts/check-database.sh
```

當前數據庫已包含：
- ✅ 9 條區塊鏈配置
- ✅ 257 個協議
- ✅ 6124 個流動性池
- ✅ 系統配置項
- ✅ AI Agent 相關表（`ai_think_log`、`ai_memory`、`ai_decisions`，用於思考日誌、記憶與決策反饋）

### Dashboard 功能

訪問 http://localhost:3002 可以看到：

1. **總覽頁面** `/`
   - 總鎖倉量（TVL）
   - 總年化收益（APR）
   - 活躍池子數量
   - 24小時交易量
   - 市場情緒（需 AI Engine 運行）與 AI 大腦思考日誌（來自 `ai_think_log`）

2. **池子列表** `/pools`
   - 篩選：按鏈、最低 TVL、健康分
   - 排序：APR、TVL、健康分
   - 搜索：協議名、代幣符號

3. **持倉管理** `/positions`
   - 當前持倉
   - 未實現盈虧
   - 已實現盈虧

4. **策略管理** `/strategies`
   - 策略配置
   - 策略表現

5. **設置頁面** `/settings`
   - 系統參數配置
   - 風險管理設置
   - 錢包地址管理

---

## 🤖 AI Agent 與數據庫

- **全新安裝**：`init.sql` 已包含 AI Agent 所需的三張表（`ai_think_log`、`ai_memory`、`ai_decisions`），無需額外操作。
- **已有數據庫（在加入 AI 表之前創建）**：請手動執行遷移以創建上述表：
  ```bash
  psql -h localhost -p 5433 -U defi -d defi_yield -f infra/postgres/migrations/004_ai_agent_tables.sql
  ```
  若使用 Docker：`docker compose exec -T timescaledb psql -U defi -d defi_yield < infra/postgres/migrations/004_ai_agent_tables.sql`

---

## 🚀 進階功能

### 啟動桌面應用

```bash
cd packages/desktop
pnpm install
pnpm start
```

### 打包桌面應用

```bash
cd packages/desktop

# 打包當前平台
pnpm build

# 打包所有平台（需在 Mac 上運行才能打包 Mac 版本）
pnpm build:all
```

### 發布更新

```bash
# 1. 更新版本號
cd packages/desktop
npm version patch  # 或 minor / major

# 2. 打包
pnpm build

# 3. 部署到更新服務器
cd ../..
bash scripts/deploy-update.sh
```

詳細流程見：[桌面應用發布流程](../packages/desktop/RELEASE.md)

---

## 🛠️ 開發工作流

### 常用命令

```bash
# === 數據庫管理 ===
bash scripts/start-database.sh          # 啟動數據庫
bash scripts/check-database.sh          # 檢查數據庫
docker compose logs -f timescaledb      # 查看數據庫日誌
docker compose restart timescaledb      # 重啟數據庫

# === Dashboard 開發 ===
cd packages/dashboard
pnpm dev                                # 開發模式
pnpm build                              # 生產構建
pnpm lint                               # 代碼檢查

# === 桌面應用開發 ===
cd packages/desktop
pnpm start                              # 啟動應用
pnpm build                              # 打包應用
```

### 目錄結構

```
dapp/
├── ai-engine/              # AI 引擎（Python）
├── docs/                   # 文檔
│   ├── 快速开始.md         # 本文檔
│   ├── 数据库启动指南.md   # 數據庫詳細說明
│   └── GIT与更新配置说明.md # Git 和更新配置
├── infra/                  # 基礎設施配置
│   ├── postgres/          # 數據庫初始化腳本
│   ├── redis/             # Redis 配置
│   └── grafana/           # Grafana 儀表板
├── packages/
│   ├── dashboard/         # Web Dashboard（Next.js）
│   ├── desktop/           # 桌面客戶端（Electron）
│   ├── scanner/           # 池子掃描器
│   ├── executor/          # 交易執行器
│   └── adapters/          # 協議適配器
├── scripts/
│   ├── start-database.sh  # 數據庫啟動腳本
│   ├── check-database.sh  # 數據庫檢查腳本
│   ├── deploy-update.sh   # 更新部署腳本
│   └── setup-nginx-updates.sh  # Nginx 配置生成器
├── .env                   # 環境變量（本地配置）
├── .env.example           # 環境變量模板
└── docker-compose.yml     # Docker 服務定義
```

---

## 🔧 常見問題

### Q1: Dashboard 連不上數據庫？

**解決方案：**

```bash
# 1. 確認數據庫運行
docker compose ps timescaledb
# 狀態應該是 "Up (healthy)"

# 2. 檢查配置
grep POSTGRES_ .env

# 3. 驗證連接
bash scripts/check-database.sh

# 4. 重啟 Dashboard
cd packages/dashboard
pnpm dev
```

### Q2: 數據庫表為空？

當前數據庫已有數據（6124 個池子），如需更新：

```bash
# 運行掃描器獲取最新數據
cd packages/scanner
pnpm start

# 或生成模擬數據
pnpm seed
```

### Q3: 端口被佔用？

默認端口：
- Dashboard: 3002
- 數據庫: 5433
- Redis: 6379
- Grafana: 3003

修改 `.env` 文件可更改：

```bash
DASHBOARD_PORT=3002
POSTGRES_PORT=5433
REDIS_PORT=6379
```

### Q4: Docker 容器啟動失敗？

```bash
# 查看日誌
docker compose logs timescaledb

# 重新創建容器
docker compose down
docker compose up -d timescaledb

# 如果數據損壞（會清空數據！）
docker compose down -v
docker compose up -d timescaledb
```

---

## 📚 更多文檔

### 核心文檔
- 📖 [設置指南](./设置指南.md) - 環境變量、數據庫、服務啟動與所有配置項詳解
- 📖 [AI 能力說明](./AI能力说明.md) - 市場情緒、Alpha、記憶、反饋、思考循環等 AI 模塊說明
- 📖 [數據庫啟動指南](./数据库启动指南.md) - 數據庫詳細配置和管理
- 📖 [Git 與更新配置](./GIT与更新配置说明.md) - Git 倉庫和自動更新配置
- 📖 [桌面應用發布流程](../packages/desktop/RELEASE.md) - 版本發布詳細步驟
- 📖 [主 README](../README.md) - 項目總體介紹

### 技術棧
- **前端**: Next.js 16 + React 19 + TypeScript
- **桌面端**: Electron + electron-builder
- **數據庫**: TimescaleDB (PostgreSQL 16 + 時序擴展)
- **緩存**: Redis 7
- **監控**: Grafana
- **AI 引擎**: Python + FastAPI

---

## 🎯 下一步

完成快速開始後，你可以：

1. **探索數據**
   - 在 Dashboard 中瀏覽不同鏈上的池子
   - 查看 APR 最高的機會
   - 分析風險指標

2. **配置策略**
   - 訪問 `/strategies` 設置自動化策略
   - 配置風險參數（止損、交易上限等）

3. **本地開發**
   - 修改 Dashboard 代碼並實時預覽
   - 開發新的協議適配器
   - 擴展 AI 引擎功能

4. **部署生產**
   - 配置 Git 倉庫推送代碼
   - 設置自動更新服務器
   - 打包並發布桌面應用

---

**祝開發愉快！** 🚀

如有問題，請查看：
- 📁 [docs/](.) 目錄下的其他文檔
- 🐛 項目 Issues
- 💬 開發團隊聯繫方式
